[project]
name = "financial-tracker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["pdfplumber>=0.11", "httpx>=0.28"]

[project.optional-dependencies]
dev = ["pytest>=9"]

[tool.pytest.ini_options]
testpaths = ["tests"]
